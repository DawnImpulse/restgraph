/**
 * export the default function
 *
 * @param restGraph
 * @param data
 * @return object - mapped data
 */
export declare function mapData(restGraph: string, data: object): object;
/**
 * create restgraph map from a data
 * @param data
 */
export declare function createMap(data: object): string;
